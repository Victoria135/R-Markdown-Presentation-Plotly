---
title: "project 9.2"
author: "Victoria"
date: "2026-05-15"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

Let's make a plot for length and width of sepal in iris data!

```{r iris}
library(plotly)

plot_ly(
  iris,
  x = ~Sepal.Length,
  y = ~Sepal.Width,
  type = "scatter",
  mode = "markers"
)
```
